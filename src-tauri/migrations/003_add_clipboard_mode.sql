-- Add clipboard_mode setting
-- This setting controls whether to copy GIF files or URLs to clipboard

-- The setting is stored in the settings table as a key-value pair
-- No schema changes needed as settings uses flexible key-value storage

-- Default value will be 'file' mode, set in Rust code
